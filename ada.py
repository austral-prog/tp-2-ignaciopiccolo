def ada():
    first_name = "AdA"
    last_name = "LoVeLAce"
    lower_case = f"{first_name.lower()} {last_name.lower()}"
    title_case = f"{first_name.title()} {last_name.title()}"
    upper_case = f"{first_name.upper()} {last_name.upper()}"
    tabbed_lower_case = f"\t{first_name.lower()} {last_name.lower()}"
    print(lower_case)
    print(title_case)
    print(upper_case)
    print(tabbed_lower_case)
